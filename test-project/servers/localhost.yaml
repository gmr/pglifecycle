---
name: localhost
foreign_data_wrapper: postgres_fdw
options:
  host: localhost
  port: 5432
  user: fdw_user
  dbname: postgres
dependencies:
  extensions:
    - postgres_fdw
comment: Example FDW that conencts back to the same server on a different DB
